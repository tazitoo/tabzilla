###### directory where results will be written
#output_dir: ./results/
# we should stash the explanations in the same directory as the model, right?
# we should use the splits for the dataset
# the only thing we should specify here is the number of samples to include in "data" to train the explainer
#
name: kernelSHAP
seed: 123
trials: 1
subsample_percentage: 1.0
fractions: [0.25, 0.5, 0.75, 1.0]
overwrite: False
